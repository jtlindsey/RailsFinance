  <div class="col-md-12">
    <div class="panel panel-danger">
      <div class="panel-heading">User Details</div>
      <div class="panel-body" >

        <div class="row visible-sm-block visible-md-block visible-lg-block">
          <div class="col-md-12">
            <table id="transactions" class="transactions-for-checking table table-striped table-hover table-condensed">
              <thead>
                <tr>
                  <th>Created at</th>
                  <th>Visit ID</th>
                  <th>User ID</th>
                  <th>Email</th>
                  <th>Ip Address</th>
                </tr>
              </thead>

              <tbody>
                <% @visits.each do |visit| %>
                  <tr>
                    <td><%= visit.created_at %></td>
                    <td><%= visit.id %></td>
                    <td><%= visit.user_id %></td>
                    <td><%= visit.user_email %></td>
                    <td><%= visit.user_ip %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div> 

        <!-- Mobile -->
        <div class="row visible-xs-block">
          <div class="col-md-12">

            <div class="panel-primary">
              <% @visits.each do |visit| %>
                <table class="table table-striped">
                <tbody>
                  <tr style="background: black"><td></td><td><%= "" %></td></tr>
                  <tr><td>Created at</td><td><%= visit.created_at %></td></tr>
                  <tr><td>Visit Id</td><td><%= visit.id %></td></tr>                  
                  <tr><td>User ID</td><td><%= visit.user_id %></td></tr>
                  <tr><td>Email</td><td><%= truncate(visit.user_email) %></td></tr>
                  <tr><td>Ip Address</td><td><%= visit.user_ip %></td></tr>
                </tbody>
                </table>
              <% end %>
            </div>

          </div>
        </div>
        <!-- End Mobile -->

      </div>
    </div>
  </div>


<script>
// for dataTables
$(document).ready(function() {
  var custom_table = $('#transactions').dataTable();
  // sort a column descending (at position 0 in the array (base 0)
  custom_table.fnSort( [ [0,'desc'] ] );
} );
</script>