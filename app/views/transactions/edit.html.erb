<h1>Editing Transaction</h1>

<%= render 'form' %>

<%= link_to 'Show', account_transaction_path(:account_id => @account.id, :id => @transaction.id) %> |
<%= link_to 'Back', account_transactions_path(:account_id => @account.id) %>

